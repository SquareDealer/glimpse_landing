{
  "name": "glimpse-landing",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run build && node scripts/check.mjs",
    "preview": "python3 -m http.server 4173 --directory dist",
    "start": "python3 -m http.server 4173 --directory ."
  },
  "engines": {
    "node": ">=18"
  }
}
